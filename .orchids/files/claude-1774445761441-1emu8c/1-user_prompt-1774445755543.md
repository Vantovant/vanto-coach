You must reconnect using GitHub CLI in a NEW clean clone inside your own workspace.

Do NOT use your current internal Orchids repo as source of truth.
Do NOT trust your current origin remote.
Do NOT implement anything yet.
Do NOT push, commit, or edit anything yet.

Real source of truth:
GitHub repo: Vantovant/vanto-coach
Branch: main

MISSION
Create a fresh read-only GitHub-backed workspace using gh CLI, then prove what you can actually see.

RULES
- No hallucinations
- No inferred file structure
- No implementation yet
- No pushing
- No committing
- No editing
- Only authenticate, clone, inspect, and report verified facts

STEP 1 — CHECK GH CLI
Run:
1. gh --version
2. gh auth status

If not authenticated:
Run:
3. gh auth login --web --git-protocol https
4. gh auth setup-git

Important:
- Use github.com
- Use HTTPS
- Authenticate Git when prompted
- If browser login fails, stop and show the exact failure
- Do NOT paste tokens into chat

STEP 2 — CLONE A FRESH GITHUB COPY
Create a brand-new folder from GitHub, not from Orchids internal storage.

Run:
5. gh repo clone Vantovant/vanto-coach vanto-coach-github
6. cd vanto-coach-github

STEP 3 — PROVE CONNECTION WITH RAW OUTPUTS ONLY
Run and return raw output for:
1. pwd
2. git remote -v
3. gh repo view Vantovant/vanto-coach --json name,defaultBranchRef,url,isPrivate,viewerPermission
4. git branch --show-current
5. git status --short
6. git log --oneline -8
7. git ls-files | findstr /I ".env .orchids"
8. git ls-files src/components/tabs
9. git ls-files src/hooks
10. git ls-files src/lib
11. gh api repos/Vantovant/vanto-coach/contents/src/components/tabs?ref=main
12. gh api repos/Vantovant/vanto-coach/contents/src/hooks?ref=main
13. gh api repos/Vantovant/vanto-coach/contents/src/lib?ref=main

STEP 4 — REPORT ONLY VERIFIED FACTS
After the raw outputs, give only these sections:

1. CONNECTION CONFIRMED OR NOT CONFIRMED
- Are you now truly connected to the real GitHub repo?
- Are you inside the fresh GitHub clone?

2. REPO TRUTH SUMMARY
- What top-level structure do you actually see?
- What files do you actually see in:
  - src/components/tabs
  - src/hooks
  - src/lib

3. CURRENT BRANCH AND COMMITS
- What branch is checked out?
- What are the latest visible commits?

4. TRACKED ENV / ORCHIDS STATUS
- Is .env.example tracked?
- Are any secret env files tracked?
- Are any .orchids files tracked?

5. SAFEST NEXT TASK
- Based only on this verified clone, what is the safest next engineering task?
- Do not implement yet

MOST IMPORTANT RULE
If a command fails, show the exact failure.
If a file is not returned by git or gh output, do not claim it exists.
If you are unsure, say “not verified”.