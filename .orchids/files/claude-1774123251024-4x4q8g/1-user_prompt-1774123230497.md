Continue from the real GitHub repository only:
https://github.com/Vantovant/vanto-coach

Local repo:
D:\GitHub\vanto-coach

Branch rule:
- Work on main
- Push to origin/main only
- Do NOT push to master
- Do NOT redesign the UI
- Do NOT touch .env.local
- Do NOT commit Orchids artifacts, tar.gz files, or local-only files

Current verified status:
- Gate 1 passed locally
- Gate 2 passed on Vercel/live
- Gate 3 passed locally
- Diary core is now production-verified:
  - record works
  - save works
  - refresh works
  - replay works
  - delete works

Next task: Gate 4

Gate 4 goal:
Replace remaining mock-driven core flows with real persisted data, starting with the most important user-facing screens.

Priority order:
1. TodayTab briefing
2. Today scripture-for-today flow
3. real memory writing into coach_memories
4. MemoryTab reading real memory rows

Important context:
The app already has Supabase auth, session persistence, audio storage, and coach session saving in place.
The next need is to remove mixed mock/live behavior from core surfaces.

What to do:
1. Audit the current TodayTab implementation and identify exactly which parts are still powered by mock data
2. Replace mockDailyBriefing / equivalent placeholder briefing logic with real data derived from persisted user/session context
3. Replace scripture-for-today mock flow with a real implementation appropriate to the current architecture
4. Inspect the diary processing pipeline and write real memory rows into coach_memories
5. Update MemoryTab so it reads real memory rows from Supabase and displays them correctly
6. Keep UI and visual design unchanged
7. Do not add new product features yet
8. Do not redesign tabs
9. Do not change unrelated modules

Testing required:
- Today tab loads real data without mock placeholders
- Scripture for today is real, not hardcoded mock content
- creating a diary session can create memory rows where appropriate
- MemoryTab shows persisted memory rows
- refresh preserves the data
- no regressions to diary save/playback/delete flow

Git requirements:
- commit to main
- push to origin/main only
- keep repo clean
- do not commit .env.local, .orchids, or tar.gz files

Return format:
- what mock paths were removed
- what real data paths replaced them
- files changed
- commit hash
- exact local test steps
- exact live/Vercel test steps