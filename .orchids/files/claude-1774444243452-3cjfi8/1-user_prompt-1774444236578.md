VANTO COACH — PRODUCTION HARDENING SPRINT 6

Continue from the real GitHub repository after Sprint 5.

CURRENT STATUS
Sprint 1 is complete:
- global toaster mounted
- monitoring layer added
- 45s timeout protection added
- timedOut UI state added
- retry panels added
- save/delete feedback improved

Sprint 2 is complete:
- retry AI processing persists results back into Supabase
- retry updates in-memory session state immediately
- selected session detail panel updates live
- retry is idempotent and double-taps are blocked

Sprint 3 is complete:
- initial diary history is paginated
- older sessions load incrementally
- search/filtering is DB-driven
- Today uses lightweight bounded queries
- prayer_points consistency was hardened

Sprint 4 is complete:
- retry now repairs structured entry data
- coach_structured_entries is upserted during retry
- SessionCard action/prayer badges update immediately after retry
- selected session detail view heals immediately without reload

Sprint 5 is complete:
- MemoryTab pin/archive writes are real
- ActionPlansTab per-item and bulk status writes are real
- SettingsTab persists real settings
- VantoOS badge is now truthful (“Coming Soon”)

MISSION FOR THIS SPRINT
Remove the remaining silent or inert controls so the product gives clear feedback for real writes and does not leave one-way or fake actions in the UI.

STRICT RULES
- Use the actual GitHub repo as source of truth
- Verify real files before changing anything
- Do not redesign the UI
- Do not add unrelated features
- Do not weaken security, RLS, or storage privacy
- Do not commit .env.local
- Do not commit .orchids
- Do not commit junk artifacts
- Preserve the current premium experience

SPRINT OBJECTIVE
Close the remaining UX-truthfulness gaps in MemoryTab, ActionPlansTab, and SettingsTab.

IMPLEMENT IN THIS ORDER

1. INSPECT THE REMAINING SILENT / INERT CONTROLS
Inspect the real repo and identify:
- MemoryTab pin/archive success and failure paths
- ActionPlansTab per-item and bulk status-change success and failure paths
- whether Export All Data in SettingsTab is still inert
- whether MemoryTab has any UI for archived items / unarchive
- any other remaining production-path controls that still do nothing or give no feedback

Before major edits:
summarize the exact files, components, hooks, and helpers you will touch.

2. ADD TOAST FEEDBACK FOR MEMORYTAB WRITES
Wrap real DB writes with clear success/failure feedback for:
- pin
- unpin
- archive
- unarchive (if added in this sprint)

Requirements:
- success toast on confirmed DB write
- error toast on failure
- no false success before persistence completes
- preserve in-memory patching without reload

3. ADD TOAST FEEDBACK FOR ACTIONPLANSTAB WRITES
Wrap real DB writes with clear success/failure feedback for:
- per-item approve
- per-item reject
- move to pending
- bulk approve
- bulk reject

Requirements:
- success toast where helpful but not noisy
- error toast on failure
- no false success before DB write completes
- preserve current state patching behavior

4. ADD A REAL UNARCHIVE FLOW IN MEMORYTAB
Implement a minimal truthful archived-memory recovery flow.

Requirements:
- provide a way to view archived memories
- provide an unarchive action
- unarchive must write to Supabase using the real data layer
- state should patch immediately without hard reload
- preserve current premium feel
- do not redesign the whole tab

5. RESOLVE THE EXPORT ALL DATA CONTROL
Inspect SettingsTab and determine whether “Export All Data” is still inert.

If it is inert:
- either implement a real export path using existing real data sources
- or make the UI truthful by marking it clearly as coming soon / unavailable

Prefer a real export if the repo structure already makes this straightforward and safe.

If implementing export:
- keep it user-scoped
- export only the authenticated user’s data
- use a safe JSON or CSV approach based on the real schema
- include at least sessions, memories, and action items if feasible
- do not over-engineer beyond the current product scope

6. PRESERVE ALL PRIOR HARDENING GAINS
Make sure the following still work exactly as before:
- retry processing
- structured entry repair
- paginated diary loading
- DB-driven search/filter
- timeout handling
- monitoring hooks
- settings persistence
- no hard reloads

7. RUN REGRESSION CHECKS
After implementation verify:
- MemoryTab pin/unpin shows correct feedback
- MemoryTab archive/unarchive shows correct feedback
- ActionPlansTab per-item actions show correct feedback
- ActionPlansTab bulk actions show correct feedback
- archived memory recovery works
- Export All Data is either real or truthfully disabled
- Diary still works
- retry still works
- Today still works
- Insights still works
- search/filter still works
- no duplicate items appear
- no full reloads were introduced

OUTPUT REQUIRED FROM YOU
Before major edits:
1. inspect the real repo
2. identify exact files/hooks/helpers to touch
3. list the remaining silent or inert controls
4. explain the implementation strategy briefly

After implementation provide:
1. what was changed
2. files touched
3. how MemoryTab feedback and unarchive now work
4. how ActionPlansTab feedback now works
5. whether Export All Data is now real or truthfully disabled
6. regression checks run
7. known risks
8. recommended Sprint 7

DEFINITION OF DONE
This sprint is successful only if:
- real writes in MemoryTab and ActionPlansTab give clear user feedback
- archived memories can be recovered through the UI
- Export All Data is no longer an inert misleading control
- all prior hardening gains remain intact
- no redesign or feature sprawl was introduced

FINAL INSTRUCTION
Think like a senior continuation engineer:
remove the last silent edges,
make every control honest,
and keep the product calm, real, and trustworthy.