VANTO COACH — BUILD REPAIR ONLY

Use the real GitHub repo only:
https://github.com/Vantovant/vanto-coach

Branch:
main

Current top commit:
e303ff2

Do NOT start the Today/Scripture/Insights bug-fix task yet.

MISSION
Repair the broken build caused by missing exports in src/lib/supabase/db.ts.

CURRENT VERIFIED BUILD FAILURE
The repo currently fails type-check because these imports exist but their exports are missing from db.ts:

- src/components/tabs/InsightsTab.tsx imports getRecentSessions
- src/components/tabs/TodayTab.tsx imports getRecentSessions
- src/hooks/useSessions.ts imports getSessionsPage
- src/hooks/useSessions.ts imports SESSION_PAGE_SIZE

But src/lib/supabase/db.ts does not export those members.

STRICT RULES
- Fix only the build mismatch
- No redesign
- No unrelated feature work
- No UI changes unless strictly necessary for the build
- Preserve Sprint 6 work
- Reconcile db.ts with the current consumers
- If the helpers existed in an earlier sprint version, restore them correctly
- Do not fake signatures; match the actual consuming code

WHAT YOU MUST DO FIRST
1. Inspect:
   - src/lib/supabase/db.ts
   - src/hooks/useSessions.ts
   - src/components/tabs/TodayTab.tsx
   - src/components/tabs/InsightsTab.tsx
2. Identify the exact expected signatures/types for:
   - getRecentSessions
   - getSessionsPage
   - SESSION_PAGE_SIZE
   - SessionsPageResult (if needed)
3. Repair db.ts so the imports are valid and the build passes

OUTPUT REQUIRED
After fixing:
1. exact files changed
2. exact exports restored/added
3. commit hash
4. result of local build
5. short note confirming no other features were changed

FINAL RULE
This is a build-repair task only.
Do not start the larger UI hardening task yet.