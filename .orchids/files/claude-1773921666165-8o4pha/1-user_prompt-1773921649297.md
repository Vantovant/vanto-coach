Continue from the audited repository exactly as it exists.

Do not redesign the app.
Do not rebuild from scratch.
Preserve the current Vanto Coach UI, tab structure, diary flow, scripture flow, and premium styling.

Next phase: implement the Supabase production foundation.

Priority order:
1. add Supabase authentication
2. add database schema for coach sessions and related data
3. add row level security
4. add storage for recorded audio
5. persist the current diary/session flow to Supabase
6. keep raw transcript, cleaned transcript, summary, mood, scripture refs, prayer points, and action items saved per user

Use the actual audited codebase as the source of truth.

Before making changes:
- summarize the exact files you will touch
- explain how you will preserve the current UI and behavior
- identify any mock-data dependencies that must be replaced carefully

Implementation requirements:
- use a clean service/data-access layer
- avoid scattering Supabase calls across UI components
- preserve current tabs and layout
- preserve source records at all times:
  - audio
  - raw transcript
  - cleaned transcript
  - summary

Minimum schema:
- profiles
- coach_sessions
- coach_structured_entries
- coach_action_items
- coach_scripture_refs
- coach_prayer_points
- coach_memories

Important:
- do not guess beyond the repo
- continue from the actual code
- make careful production-oriented changes only