Continue from the real GitHub repository only:
https://github.com/Vantovant/vanto-coach

Local repo:
D:\GitHub\vanto-coach

Branch rule:
- Work on main
- Push to origin/main only
- Do NOT push to master
- Do NOT redesign the UI
- Do NOT change unrelated features
- Do NOT touch .env.local

Current status:
- Gate 1 passed locally
- Gate 2 passed on Vercel/live
- Fresh diary entries no longer duplicate
- Fresh diary entries no longer show NaN:NaN
- Fresh diary audio plays after refresh
- The remaining next task is Gate 3

Gate 3 goal:
Wire the Delete Entry action properly in the Diary UI using the existing soft-delete backend function, while keeping the current UI unchanged.

Important confirmed fact from the repo:
The delete service already exists in:
src/lib/supabase/db.ts

Exact function:
softDeleteSession(id: string): Promise<boolean>

What must be done:
1. In src/components/diary/SessionDetail.tsx
   - Add an optional prop:
     onDelete?: (id: string) => void | Promise<void>;
   - Update the SessionDetail component signature so it receives onDelete
   - Keep the X button exactly as close-only
   - Wire the “Delete Entry” DropdownMenuItem so it actually calls:
     onDelete?.(session.id)
   - Use onSelect with preventDefault so the menu behaves correctly
   - Do not redesign the file

2. In src/components/tabs/DiaryTab.tsx
   - Import softDeleteSession from @/lib/supabase/db
   - Add a delete handler like:
     - confirm with window.confirm("Delete this entry?")
     - call softDeleteSession(id)
     - if failure, show alert("Failed to delete entry")
     - if success, update the UI cleanly
   - Preferred behavior:
     remove the deleted entry from the list immediately without requiring manual refresh
   - If selectedSession is the one deleted, clear it safely
   - If a simple safe first pass is needed, reload after success, but only if immediate UI removal is harder
   - Pass onDelete={handleDeleteSession} into SessionDetail where it is rendered

3. Preserve these rules:
   - Keep existing styling
   - Keep existing layout
   - Keep current audio behavior intact
   - Do not break the recently fixed duplicate/audio-reload behavior
   - Do not change unrelated tabs

4. Testing required
   After making the changes, test locally and confirm:
   - open a diary entry
   - click the three-dot menu
   - click Delete Entry
   - confirm delete
   - entry disappears
   - no crash
   - other diary entries still open normally
   - X button still only closes the panel
   - diary save/playback behavior still works

5. Git requirements
   - Commit the changes to main
   - Push to origin/main
   - Do not leave the fix on master
   - Return the commit hash

6. Return format
   Return only:
   - root cause
   - files changed
   - exact behavior now
   - commit hash
   - short local test result
   - short live/Vercel test recommendation

Reminder:
This is a Next.js app. GitHub is the source of truth. Do not hallucinate structure. Verify against the real files before editing.